# azure-meteoschweiz-pipeline
Learning to use Azure by playing around with data from MeteoSchweiz. Lessons generated by Claude (Sonnet 5).

Run `python scripts/setup/download_data.py` to populate `data/raw/`.
